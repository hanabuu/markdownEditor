{
  "name": "testelectron2",
  "version": "1.0.0",
  "description": "TestElectron2",
  "main": "index.js",
  "scripts": {
    "start": "node_modules/.bin/electron ./src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-mac": "node_modules/.bin/electron-packager ./src --asar --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds --darwinDarkModeSupport=true --extraResources=images",
    "package-win": "node_modules/.bin/electron-packager ./src --asar --overwrite --platform=win32 --arch=ia32 --prune=true --out=release-builds --extraResources=images"
  },
  "keywords": [
    "electron"
  ],
  "author": "hanabusa",
  "license": "ISC",
  "dependencies": {
    "electron": "^12.0.9",
    "electron-packager": "^15.2.0"
  }
}
